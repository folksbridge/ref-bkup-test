import React from 'react';
import ReactDOM from 'react-dom';

import App from './App.js';

//import './index.html';
/* eslint no-undef: 0 */
// if (process.env.NODE_ENV === 'development') {
//   require('./index.html');
// }

//for demo eslint: no-unused-var
let test;

ReactDOM.render(<App />, document.getElementById('root'));
